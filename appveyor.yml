version: "{build}"

os:
  - Visual Studio 2015

install:
  - pip install conan
  - git clone --recursive https://github.com/vladasz/utils			
  - py Build.py --ide

build:
  project: build/TestEngine.sln
  parallel: true                        
  verbosity: normal
