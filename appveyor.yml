version: "{build}"

os:
  - Visual Studio 2017

install:
  - git submodule update --init --recursive  
  - pip install conan
  - py build.py --ide
  
configuration:
  - Release

build:
  project: build/TestEngine.sln
  parallel: true                        
  verbosity: normal
