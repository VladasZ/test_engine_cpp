version: "{build}"

os:
  - Visual Studio 2019

install:
  - git clone https://github.com/vladasz/deploy
  - py deploy/deploy.py
  - py build.py --clean --prepare --ide --debug --vs19
  
configuration:
  - Release

build:
  project: build/test_engine.sln
  parallel: true                        
  verbosity: normal
